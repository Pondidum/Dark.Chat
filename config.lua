local addon, ns = ...

local config = {
	editHeight = 15,
	chatWidth = 550,
	chatHeight = 120,
	tabHeight = 20,
	windowPadding = 15,
	enableFading = true,
	fadeTime = 20,
	linkColor = "16FF5D",
}

ns.config = config